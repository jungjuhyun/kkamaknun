# DECISIONS — 결정 전환 / 역사 로그

목적: 과거에 무엇을 왜 확정·보류·철회했는지 보존한다.

**이 파일은 current-state canonical owner가 아니다.** 각 항목의 `현재 우선`, `확정` 등은 **그 기록이 만들어진 시점의 상태**를 뜻한다. 지금 무엇이 current인지가 필요하면 `AGENTS.md`의 routing에 따라 `진행상태.md`, 활성 execution plan, 해당 domain owner를 먼저 확인한다.

상태 표기: `확정` / `현재 우선` / `유력 후보` / `보류` / `철회` / `대체됨` / `사용자 보고`.

명확한 후속 결정이 이전 결정을 바꿨다면 가능한 범위에서 `대체됨` 또는 대체 관계를 남긴다. 모든 과거 row를 복잡한 ID/graph schema로 소급 변환하지 않는다.

---

## 2026-08-19 ~ 2026-08-20

- `확정` 문자 해설(카타카나·히라가나)을 당시 주력 편성에서 제외.
- `확정` 애니 원음·표현 파생버전을 활용한 반복듣기 포맷을 설계하고 재료 수급/실측 도구를 구축.
- `철회` 잠본어를 애니 반복듣기 확장판과 통합하는 안. 수면용은 감정·효과음 조건이 달라 별도 코너로 둔다.
- `확정` 반복듣기 포맷에서 일본어 대사는 성우 원음, 한국어 해설은 자연스러운 별도 음성으로 역할 분담.
- `확정` 프로젝트 규칙은 Karpathy 공통 행동규칙과 분리해서 관리한다.

---

## 2026-08-21 ~ 2026-08-22 — 첫 콘텐츠 우선순위/포맷

- `현재 우선` **첫 콘텐츠가 가장 중요하다.** 반복듣기 포맷 세부 설계는 후속 생산 엔진 후보로 미룬다.
- `현재 우선` 첫 콘텐츠의 겉 질문은 **오랫동안 일본 콘텐츠를 접한 사용자가 자막 없이 실제 애니를 어디까지 이해하는가**다.
- `확정` 첫 콘텐츠의 상위 목적은 단순 청해 점수가 아니라 **사용자가 일본어 공부를 어디서부터 시작해야 하는지 진단하는 것**이다.
- `확정` 같은 유형의 시청자에게 사용자의 결과를 그대로 따라 하라고 하지 않는다. **사용자가 자기 구멍을 찾는 진단 과정을 보여주고, 시청자가 그 방법을 자기에게 대입하게 한다.**
- `철회/후순위` HelloTalk 라이브 방에서 재미있는 상황이 나올 때까지 기다리는 방식. 사건 발생을 통제할 수 없어 제작시간이 비정상적으로 늘어날 수 있다.
- `유력 후보` 작품은 `귀멸의 칼날 TV 애니`. 과거 일부 봤고 세부 기억은 흐릿하며 자막 없이 본 적은 없다. 정확한 화수는 아직 최종 확정 아님.
- `유력 후보` 과거 기억 오염이 큰 화를 쓸 경우 시청 전 기억나는 내용을 짧게 잠그는 `pre-memory` 기록을 사용한다.

---

## 2026-08-22 — 촬영 프로토콜

- `확정` 촬영은 **자막 OFF → 의미가 들리거나 중요한데 못 알아들었다고 느끼면 SPACE → 들린 일본어 소리를 가능한 범위에서 그대로 재현 → 내가 이해한 뜻 말하기 → 정답 확인 없이 계속 재생** 구조다.
- `확정` 촬영 중 자막/사전/검색으로 정답을 확인하지 않는다. 촬영본의 첫 청취 반응을 오염시키지 않는 것이 우선이다.
- `확정` 사용자는 평소 혼잣말을 많이 하지 않으므로 계속 떠드는 진행을 강요하지 않는다. **반응이 생길 때만 말한다.** 침묵은 편집에서 제거 가능한 원재료다.
- `확정` 어설프게 일본어 소리를 따라 말하는 것은 단순 개그가 아니라 **실제로 어떤 소리를 잡았는지와 그 소리로 어떤 뜻을 만들었는지를 분리하는 진단 데이터**다.
- `철회` 고정 체크포인트마다 줄거리를 요약하고 마지막에 전체 이야기를 재구성하는 것을 촬영의 중심 장치로 삼는 이전안. 최신 포맷에서는 SPACE 즉석 반응이 기본 원재료다.

---

## 2026-08-22 — 후편집/진단

- `확정` 촬영본의 사건은 `정확히 들음 / 부분 청해 / 문맥 추론 / 오답 / 미청취`로 분류한다.
- `확정` 오답 원인은 감으로 단정하지 않고 실제 자막을 본 뒤 단계적으로 확인한다.
- `확정` 진단 대분류는 `어휘 / 문법·조사·활용 / 소리 인식 / 문자·한자 / 문맥 의존 / 기타`다.
- `확정` 한 오답에 원인이 여러 개 겹칠 수 있다. 필요하면 `주원인 + 보조원인` 또는 `복합`으로 기록한다.
- `확정` 영상의 재미는 일본어 공부 설명에서 억지로 만들지 않는다. **사용자의 즉석 판단과 실제 정답의 일치/충돌, 예상 밖 성공, 자신 있는 오답, 문맥으로 맞힌 순간**에서 만든다.
- `확정` 촬영본은 완성 영상이 아니라 **실험 데이터 + 리액션 원재료**다. 완성본의 서사는 촬영 후 실제 결과에서 발견한다.
- `확정` 결과는 임의의 `청해 70%` 같은 체감 점수보다 **무엇 때문에 막혔는지와 가장 큰 학습 구멍이 무엇인지**를 우선한다.
- `현재 우선` 결론은 `지피지기면 백전백승 → 내가 부족한 지점을 알았으니 그 지점부터 채운다`로 다음 학습과 연결한다.

---

## 2026-08-21 ~ 2026-08-23 — 화면/익명성/장비

- `현재 우선` 사용자 리액션 화면을 크게, 애니 화면은 PIP로 함께 보여주는 구도.
- `철회` 실제 얼굴 위에 눈/입만 캐릭터처럼 합성하는 방식. 테스트 이미지에서 시각적으로 어색했다.
- `철회` 완전 디지털 캐릭터 얼굴 트래킹을 첫 영상 전에 구축하는 안. 첫 영상 전에 들일 이유가 약하다.
- `철회` 실물 전면 복면을 고정 캐릭터로 쓰는 방향. 실제 얼굴 프린트 복면은 얇고 비치며, 속눈썹을 눌러 눈을 찌르는 등 장시간 착용이 불편했고 눈 구멍을 잘라도 실용성이 충분히 개선되지 않았다.
- `확정` 현재는 **얼굴을 그대로 보여주는 방향**으로 전환한다.
- `확정` 현재 FHD 웹캠의 약간 흐릿하고 생활감 있는 화면을 사용자 본인이 선호하므로, 미러리스 업그레이드는 현재 필요하지 않다.
- `확정` 과도한 스튜디오/배경 연출은 하지 않는다. 필요한 장비는 실제 불편을 해결하는 것만 산다.
- `현재 우선` 카메라 각도 해결용으로 약 104cm 데스크 클램프 스탠드 + 각도 조절 볼헤드 조합을 사용/구매 후보로 둔다.
- `현재 우선` 조명은 현재 화면 결을 유지한다면 추가 구매하지 않는다.
- `확정` 본촬영 OBS 오디오는 가능하면 `전체 믹스 백업 / 마이크 / 애니·데스크톱` 분리 트랙으로 녹음한다.

---

## 2026-08-23 — 파일럿에서 나온 실전 보정

- `사용자 보고` 약 3분 파일럿에서 재미있는 부분이 일부 발생했다. AI가 해당 영상을 실제로 분석했다고 간주하지 않는다.
- `사용자 보고` 정확한 대사 끝 지점에 맞춰 SPACE를 누르기 어렵다.
- `유력 후보` 실전 보정은 **정확한 대사 끝을 맞추기보다 생각/반응이 생기면 말하기 전에 먼저 SPACE**를 누르는 방식. 다음 촬영에서 자연스러운지 확인 후 확정한다.
- `확정` 0.5~1초 늦게 멈췄다는 이유만으로 촬영 실패로 보지 않는다. 녹화 원본이 있으므로 후편집에서 필요한 구간을 조정한다.

---

## 2026-08-23 — 후속 콘텐츠 방향

- `철회` 첫 진단 이후 `단어 공부 / 문법 공부 / 한자 공부`를 순서대로 보여주는 평범한 공부 브이로그형 진행을 기본 엔진으로 삼는 안.
- `현재 우선` 후속편은 **진단 → 병목 → 가설 → 필요한 공부/훈련 → 새로운 애니/실전 검증 → 결과 → 새 병목** 구조로 본다.
- `확정` 공부는 필요하면 촬영하지만 콘텐츠의 본체로 강제하지 않는다. 시청자에게는 가능하면 `무엇을 공부했는가`보다 **그래서 실제로 무엇이 달라졌는가**를 질문으로 판다.
- `확정` 첫 영상의 실제 진단 결과가 2편 주제를 결정한다. 결과를 보기 전에 고정 N5→N4→N3 커리큘럼을 짜지 않는다.

---

## 2026-08-23 — 기획 생산 공정/판단 보존

- `확정` 사용자가 매번 `아이디어 공격해봐`라고 별도로 지시하지 않아도 된다. **주요 기획은 AI가 제안 전에 자체 RED TEAM을 수행한다.**
- `확정` 공정은 `목표 잠금 → 원안 생성 → RED TEAM 1차 → 판정 → 수리 → RED TEAM 2차 → GO/TEST/HOLD/KILL → 사용자 제출`이다.
- `확정` 원안 생성과 공격을 같은 단계에서 섞지 않는다. RED TEAM 단계에서는 원안을 살리거나 변호할 의무가 없다.
- `대체됨` 2026-08-23 당시 주요 공격 기준을 `클릭 / 시청 지속 / 사건 밀도 / 반복 피로 / 결과 내구성 / 우연 의존성 / 제작 통제 / 사용자 적합성 / 시청자 전이 / 채널 정체성 / 반복 생산성 / 사업 적합성 / 더 단순한 대안 / 근거 수준`의 14개 축으로 기록했다. 이후 `PLANNING_PROCESS.md` 리팩터링에서 **5개 bundle(시청자 반응 / 결과 내구성 / 제작 통제 / 프로젝트 적합성 / 근거 수준)**으로 통합됐다. 현재 procedure는 `PLANNING_PROCESS.md`가 기준이다.
- `확정` 해결 가능한 공격은 구조를 수리한 뒤 다시 공격한다. 새로운 근거가 없는 한 2차 공격 이후 무한 검토하지 않는다.
- `확정` `PLANNING_PROCESS.md`를 기획 공정의 기준 문서로 사용한다.
- `확정` `JUDGMENT.md`를 재사용 가능한 판단 원칙과 이유의 보존 문서로 사용한다.
- `확정` 새 세션은 결론뿐 아니라 `왜 A보다 B를 택했는가`까지 가능한 범위에서 복원해야 한다.
- `확정` 사실·사용자 보고·추론·가설을 섞지 않는다. 확인하지 않은 파일/영상/도구 결과를 확인했다고 말하지 않는다.

---

## 2026-08-21 ~ 2026-08-23 — AI 역할/작업 규칙

- `확정` 기획·구조·시장/포맷 판단은 GPT를 주로 사용하고, 최종 나레이션 문장 다듬기는 Claude를 활용하는 역할 분담이 적합하다.
- `확정` `CLAUDE.md`의 Karpathy 규칙은 공통 상위 행동규칙으로 복원했다.
- `확정` 프로젝트 전용 규칙은 `PROJECT_RULES.md`로 분리한다.
- `확정` `CLAUDE.md`의 Karpathy 블록은 수정 금지이며 변경 감지 GitHub Actions가 있다.
- `확정` Codex/에이전트 시작 절차는 `AGENTS.md`에서 관리한다.
- `대체됨` 2026-08-23 한때 startup에 `PLANNING_PROCESS.md`와 `JUDGMENT.md`를 포함한 core 7문서 일괄 preload를 사용했다. context/memory audit 후 **small router + query-dependent progressive disclosure**로 대체한다.

---

## 2026-08-23 — Context / Memory Architecture Refactor

- `확정` repository 자체를 external memory substrate로 유지한다. 별도 active memory repo나 vector/graph DB를 선행 구축하지 않는다.
- `확정` startup fixed full preload를 폐기하고 `AGENTS.md`를 작은 router/map으로 사용한다.
- `확정` `진행상태.md`는 project-level current-state canonical owner다. 활성 execution plan이 있으면 해당 plan이 그 작업 내부 phase/local state를 소유한다.
- `확정` `USER_CONTEXT.md`는 stable user/project context만 소유하고 current first-content/gear/task state를 보유하지 않는다.
- `확정` `PROJECT_RULES.md`는 durable project invariant만 소유하며 parked/current domain implementation detail을 보유하지 않는다.
- `확정` `JUDGMENT.md`는 reusable judgment principle 중심으로 유지하고 current first-content working detail은 domain owner에 둔다.
- `확정` `DECISIONS.md`는 historical transition log이며 current truth의 두 번째 owner가 아니다.
- `확정` raw `기록/`, `실측/`, `계획.md`, git history는 삭제하지 않고 cold evidence/history로 보존한다.
- `확정` 명확한 supersession과 실제로 underdetermined인 conflict를 구분한다. 모든 충돌을 억지로 단일 current value로 만들지 않는다.
- `확정` 사용자가 새 채팅용 bootstrap prompt나 context transfer block을 관리하지 않는다. repo 접근이 가능한 세션에서 AI가 스스로 route/retrieve해야 한다.
- `확정` Phase 5 구조 변경 전 기준 commit은 `4f632705702d192a0eb188ace4d8fcee11d1d178`, backup branch는 `backup/pre-context-memory-refactor-20260823`이다.

---

## 아직 결정하지 않은 첫 콘텐츠 항목 — 당시 기록

아래는 first-content domain history다. 현재값은 `첫콘텐츠_계획.md`와 `진행상태.md`를 우선한다.

- 첫 영상에 사용할 정확한 작품/화수.
- pre-memory를 실제 본편에 사용할지 여부.
- `말하기 전에 SPACE` 보정안을 본촬영 규칙으로 완전히 확정할지 여부.
- 스페이스바 빈도가 실제 10분 이상 촬영에서 어느 정도 나오는지.
- 최종 결과를 숫자/막대까지 정량화할지, 진단 카테고리 중심으로 보여줄지.
- 첫 영상의 최종 제목/썸네일 문구.
- 반복듣기 포맷을 첫 콘텐츠 이후 몇 번째부터 투입할지.
