# DECISIONS — 사용자 결정 로그

목적: 새 세션이 과거 아이디어를 현재 확정안으로 오해하거나 이미 철회한 논의를 반복하지 않게 한다.

상태:
- `확정` — 사용자가 명시적으로 확정했거나 실행을 승인한 결정
- `현재 우선` — 현재 작업에서 우선 적용하는 방향
- `유력 후보` — 아직 확정 전이지만 우선 검토하는 후보
- `보류` — 구조상 가능하지만 지금 하지 않음
- `철회` — 현재 전제에서 사용하지 않음. 다시 살리려면 새로운 근거 필요

**AI의 `GO / TEST / HOLD / KILL` 판정은 이 상태와 다르다.**
AI 판정만으로 사용자 결정을 `확정` 처리하지 않는다.

---

## 2026-08-19 ~ 2026-08-20

- `확정` 문자 해설(카타카나·히라가나)을 당시 주력 편성에서 제외.
- `확정` 애니 원음·표현 파생버전을 활용한 반복듣기 포맷을 설계하고 재료 수급/실측 도구를 구축.
- `철회` 잠본어를 애니 반복듣기 확장판과 통합하는 안. 수면용은 감정·효과음 조건이 달라 별도 코너로 둔다.
- `확정` 반복듣기 포맷에서 일본어 대사는 성우 원음, 한국어 해설은 자연스러운 별도 음성으로 역할 분담.
- `확정` 프로젝트 규칙은 Karpathy 공통 행동규칙과 분리해서 관리한다.

---

## 2026-08-21 ~ 2026-08-22 — 첫 콘텐츠 우선순위/포맷

- `현재 우선` **첫 콘텐츠가 가장 중요하다.** 반복듣기 포맷 세부 설계는 후속 생산 엔진 후보로 미룬다.
- `현재 우선` 첫 콘텐츠의 겉 질문은 **오랫동안 일본 콘텐츠를 접한 사용자가 자막 없이 실제 애니를 어디까지 이해하는가**다.
- `확정` 첫 콘텐츠의 상위 목적은 단순 청해 점수가 아니라 **사용자가 일본어 공부를 어디서부터 시작해야 하는지 진단하는 것**이다.
- `확정` 같은 유형의 시청자에게 사용자의 결과를 그대로 따라 하라고 하지 않는다. **자기 구멍을 찾는 진단 과정을 보여주고, 시청자는 그 방법을 자기에게 대입한다.**
- `철회` HelloTalk 라이브 방에서 재미있는 상황이 나올 때까지 기다리는 것을 첫 포맷의 핵심 생산 구조로 삼는 안. 사건 발생과 제작시간을 통제하기 어렵다.
- `유력 후보` 작품은 `귀멸의 칼날 TV 애니`. 과거 일부 봤고 세부 기억은 흐릿하며 자막 없이 본 적은 없다. 정확한 화수는 미확정.
- `유력 후보` 기억 오염이 큰 화를 쓸 경우 시청 전 기억나는 내용을 짧게 잠그는 `pre-memory` 기록.

---

## 2026-08-22 — 촬영 프로토콜

- `확정` 기본 촬영은 **자막 OFF → 의미가 들리거나 중요한데 못 알아들었다고 느끼면 SPACE → 가능한 범위에서 들린 일본어 소리 재현 → 이해한 뜻 말하기 → 정답 확인 없이 계속 재생** 구조.
- `확정` 촬영 중 자막/사전/검색으로 정답을 확인하지 않는다.
- `확정` 계속 떠드는 진행을 강요하지 않는다. **반응이 생길 때만 말한다.**
- `확정` 어설픈 일본어 소리 재현은 개그만이 아니라 **소리 인식과 의미 해석을 분리하는 진단 데이터**다.
- `철회` 고정 체크포인트마다 줄거리를 요약하고 마지막에 전체 이야기를 재구성하는 것을 촬영의 중심 장치로 삼는 이전안.

---

## 2026-08-22 — 후편집/진단

- `확정` 촬영 사건은 `정확히 들음 / 부분 청해 / 문맥 추론 / 오답 / 미청취`로 본다.
- `확정` 오답 원인은 감으로 단정하지 않고 실제 자막을 본 뒤 단계적으로 확인한다.
- `확정` 진단 대분류는 `어휘 / 문법·조사·활용 / 소리 인식 / 문자·한자 / 문맥 의존 / 기타`다.
- `확정` 한 오답에 여러 원인이 겹칠 수 있으므로 필요하면 `주원인 + 보조원인` 또는 `복합`으로 기록한다.
- `확정` 재미는 공부 설명보다 **즉석 판단과 실제 정답의 일치/충돌, 예상 밖 성공, 자신 있는 오답, 문맥 추론**에서 만든다.
- `확정` 촬영본은 완성 영상이 아니라 **실험 데이터 + 리액션 원재료**다. 서사는 촬영 후 실제 결과에서 찾는다.
- `확정` 결과는 임의의 `청해 70%` 같은 체감 점수보다 **막힌 원인과 가장 큰 학습 병목**을 우선한다.

---

## 2026-08-21 ~ 2026-08-23 — 화면/익명성/장비

- `현재 우선` 사용자 리액션 화면을 크게, 애니 화면은 PIP로 함께 보여주는 구도.
- `철회` 실제 얼굴 위에 눈/입만 캐릭터처럼 합성하는 방식.
- `철회` 완전 디지털 캐릭터 얼굴 트래킹을 첫 영상 전에 구축하는 안.
- `철회` 실물 전면 복면을 고정 캐릭터로 쓰는 방향. 사용자 실착에서 장시간 불편이 컸다.
- `확정` 현재는 **얼굴을 그대로 보여주는 방향**으로 전환.
- `확정` 현재 FHD 웹캠의 약간 흐릿하고 생활감 있는 화면을 사용자 본인이 선호하므로 미러리스 업그레이드는 현재 필요하지 않다.
- `확정` 과도한 스튜디오/배경 연출은 하지 않는다. 실제 불편을 해결하는 장비만 산다.
- `현재 우선` 카메라 각도 해결용 약 104cm 데스크 클램프 스탠드 + 각도 조절 볼헤드 조합.
- `현재 우선` 현재 화면 결을 유지한다면 조명은 추가 구매하지 않는다.
- `확정` 본촬영 OBS 오디오는 가능하면 `전체 믹스 백업 / 마이크 / 애니·데스크톱` 분리 트랙으로 녹음한다.

---

## 2026-08-23 — 파일럿에서 나온 실전 보정

- `사용자 보고` 약 3분 파일럿에서 재미있는 부분이 일부 발생했다. **AI가 해당 영상을 실제로 분석했다고 간주하지 않는다.**
- `사용자 보고` 정확한 대사 끝 지점에 맞춰 SPACE를 누르기 어렵다.
- `유력 후보` 실전 보정은 **생각/반응이 생기면 말하기 전에 먼저 SPACE**를 누르는 방식. 추가 촬영에서 확인 후 확정.
- `확정` 약간 늦게 멈췄다는 이유만으로 촬영 실패로 보지 않는다. 녹화 원본이 있어 후편집으로 조정 가능하다.

---

## 2026-08-23 — 후속 콘텐츠 방향

- `철회` 첫 진단 이후 `단어 공부 / 문법 공부 / 한자 공부`를 순서대로 보여주는 공부 브이로그형 진행을 기본 엔진으로 삼는 안.
- `현재 우선` 후속편은 **진단 → 병목 → 가설 → 필요한 공부/훈련 → 새로운 애니/실전 검증 → 결과 → 새 병목** 구조로 본다.
- `확정` 공부는 필요하면 촬영하지만 콘텐츠의 본체로 강제하지 않는다. 가능하면 `무엇을 공부했는가`보다 **그래서 실제로 무엇이 달라졌는가**를 전면 질문으로 둔다.
- `확정` 첫 영상의 실제 진단 결과가 2편 주제를 결정한다. 결과 전에 N5→N4→N3 고정 커리큘럼을 짜지 않는다.

---

## 2026-08-23 — 기획 생산 공정 V1

아래는 **당시 결정 기록이며 현재는 V2로 대체됨.**

- `철회/대체` `목표 잠금 → 원안 하나 생성 → RED TEAM 1차 → 수리 → RED TEAM 2차 → 판정`을 주요 기획의 완성 공정으로 보는 V1.
- V1의 장점인 `사용자가 공격을 다시 지시하지 않아도 AI가 자기검수한다`는 원칙은 V2에 계승한다.

---

## 2026-08-23 — 기획 생산 공정 V2 / 판단 보존

- `확정` 사용자가 매번 `아이디어 공격해봐`라고 별도로 지시하지 않아도 된다. 주요 기획은 AI가 제안 전 자기검수를 수행한다.
- `확정` 단일 원안에 앵커링하지 않는다. 주요 기획은 **서로 다른 원리의 경쟁안**을 만든 뒤 상위안을 심층 공격한다.
- `확정` 현재 공정은 `CONTEXT LOCK → PROBLEM FRAME → DIVERGE → COMPETE 1 → RED TEAM A → REPAIR → RED TEAM B → 필요 시 TEST → COMPETE 2 → GO/TEST/HOLD/KILL → 사용자 결정 → 기록 → 사후교정`이다.
- `확정` 같은 모델이 생성과 공격을 모두 할 때 이를 `독립 RED TEAM`이라고 부르지 않는다. **분리 RED TEAM**으로 보고 고영향 결정은 가능하면 fresh-context 검토를 추가한다.
- `확정` RED TEAM A는 시청자/콘텐츠, RED TEAM B는 제작/사업/증거/기회비용처럼 **서로 다른 공격 축**을 맡는다.
- `확정` 각 주요 단계가 끝날 때 **Stage Exit Attack**으로 목적 적합성 / 새 결함 / 숨은 가정 / 더 단순한 길을 확인한다.
- `확정` TEST는 단순히 가장 싼 테스트가 아니라 **의사결정을 바꿀 정보량 대비 비용**이 좋은 테스트를 고른다.
- `확정` TEST 전 `가설 / 관찰 / PASS / FAIL / 애매할 때 행동 / 비용 상한 / 결정 영향`을 잠근다.
- `확정` 공격 과정에서 클릭·재미·차별성의 상방을 모두 깎아 안전한 평균안으로 만들지 않는다.
- `확정` `이 아이디어가 GO인가`와 `지금 이 제작 슬롯을 이 아이디어에 쓰는 것이 최선인가`를 분리한다.
- `확정` AI의 `GO/TEST/HOLD/KILL`과 사용자 결정을 분리한다.
- `확정` `KILL`은 현재 전제에서의 폐기이며, 다시 검토하려면 기존 근거를 깨는 새로운 증거가 필요하다.
- `확정` 주요 판단은 가능하면 실행 전 예측을 남기고 실제 결과와 비교해 판단 체계를 교정한다.

### 문서 체계 결정

- `확정` `PLANNING_PROCESS.md` — 기획 검수 공정의 소유 문서.
- `확정` `JUDGMENT_CORE.md` — 필수 부팅용 장기 판단 원칙.
- `확정` `JUDGMENT_CASES.md` — 과거 공격/수리 판단 사례. 필요할 때만 조회.
- `확정` `VALIDATION_LOG.md` — 실행 전 예측, TEST 기준, 실제 결과, 교정.
- `확정` `JUDGMENT.md` — 과거 링크 호환용 인덱스로만 유지.
- `확정` `AGENTS.md` — 필수 부팅을 경량화하고 과거 사례/검증 로그는 작업별로 조회.
- `확정` 같은 현재 사실을 여러 문서에 반복 복사하지 않고 문서별 단일 소유권을 유지한다.

---

## 2026-08-21 ~ 2026-08-23 — AI 역할/작업 규칙

- `확정` 기획·구조·시장/포맷 판단은 GPT를 주로 사용하고 최종 나레이션 문장 다듬기는 Claude를 활용하는 역할 분담이 현재 적합하다.
- `확정` `CLAUDE.md`의 Karpathy 규칙은 공통 상위 행동규칙.
- `확정` 프로젝트 전용 실패 방지 규칙은 `PROJECT_RULES.md`로 분리.
- `확정` `CLAUDE.md`의 Karpathy 블록은 수정 금지이며 변경 감지 GitHub Actions가 있다.
- `확정` Codex/에이전트 시작 절차는 `AGENTS.md`에서 관리한다.
- `확정` 사실·사용자 보고·관찰·추론·가설을 섞지 않는다. 확인하지 않은 파일/영상/도구 결과를 확인했다고 말하지 않는다.

---

## 아직 결정하지 않은 것

- 첫 영상에 사용할 정확한 작품/화수.
- pre-memory를 실제 본편에 사용할지 여부.
- `말하기 전에 SPACE` 보정안을 본촬영 규칙으로 최종 확정할지 여부.
- 10분 촬영에서 실제 사건 밀도가 충분한지.
- 60~90초 시험편집에서 반복감 없이 포맷이 살아나는지.
- 최종 결과를 숫자/막대까지 정량화할지, 진단 카테고리 중심으로 보여줄지.
- 첫 영상 최종 제목/썸네일 문구.
- 반복듣기 포맷을 첫 콘텐츠 이후 몇 번째부터 투입할지.
