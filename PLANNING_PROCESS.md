# PLANNING_PROCESS — 기획 생산 공정 V2.1

목적: 사용자가 AI 초안의 QA 담당자가 되지 않도록, **경쟁안 생성 → 분리 공격 → 수리 → 검증 → 실제 결과 교정**까지 한 공정으로 만든다.

관련 문서:
- 판단 원칙 → `JUDGMENT_CORE.md`
- 과거 판단 사례 → `JUDGMENT_CASES.md`
- 현재 상태 → `진행상태.md`
- 사용자 결정 → `DECISIONS.md`
- 예측/TEST/실제 결과 → `VALIDATION_LOG.md`

현재 사용자 지시와 새 실측이 항상 과거 문서보다 우선한다.

---

# 0. 공정이 스스로 병목이 되지 않게 하는 규칙

공정은 품질을 높이기 위한 수단이다.

### 압축 처리
다음은 전체 공정을 돌리지 않는다.
- 단순 계산
- 이미 확정된 세팅의 사용법
- 문장 한두 개 수정
- 선택지가 충분히 좁혀진 저비용 전술 결정

### 표준 처리
다음은 전체 공정을 적용한다.
- 새 콘텐츠 포맷
- 영상 핵심 구조
- 제목/훅의 큰 방향
- 후속 시리즈 엔진
- 제작방식의 큰 변경
- 기존 결정의 뒤집기

### 고영향 처리
다음은 표준 공정 + 가능하면 fresh-context 검토를 추가한다.
- 장기간 반복할 포맷 확정
- 채널 정체성 변경
- 큰 시간/비용이 들어가는 선택
- 여러 편의 방향을 동시에 바꾸는 결정

### Process Budget
> **생각하는 비용이 판정을 바꿀 수 있는 실제 테스트 비용보다 커지면 테스트로 넘어간다.**

- 체크리스트를 끝내기 위해 촬영을 늦추지 않는다.
- 동일한 공격을 새 근거 없이 반복하지 않는다.
- 후보를 3개 만드는 것이 형식적 반복밖에 안 되면 2개로 줄일 수 있다. 단, 왜 해법 공간이 좁은지 한 줄로 남긴다.

---

# 1. 전체 흐름

```text
CONTEXT LOCK
    ↓
PROBLEM FRAME
    ↓
DIVERGE
    ↓
COMPETE 1
    ↓
RED TEAM A — 시청자/콘텐츠
    ↓
REPAIR
    ↓
RED TEAM B — 제작/사업/증거/기회비용
    ↓
TEST? ── 값싼 실측이 더 낫다면 즉시 검증
    ↓
COMPETE 2
    ↓
AI VERDICT — GO / TEST / HOLD / KILL
    ↓
USER DECISION
    ↓
RECORD
    ↓
CALIBRATE — 실제 결과로 판단 체계 교정
```

**공격은 RED TEAM 단계에만 존재하지 않는다. 각 주요 단계가 끝날 때 `Stage Exit Attack`을 한다.**

---

# 2. Stage Exit Attack — 모든 주요 단계 뒤 20초 검수

방금 끝낸 단계에 대해 네 가지만 본다.

1. **목적** — 원래 문제를 실제로 전진시켰는가?
2. **신규 결함** — 해결하면서 더 큰 복잡성/비용/문제를 만들었는가?
3. **숨은 가정** — 확인하지 않은 전제를 사실처럼 끌고 가는가?
4. **단순 대안** — 방금 작업 때문에 더 싸고 직접적인 길이 보이지 않았는가?

치명적 결함이 있으면 **한 번 수리하고 다시 확인**한다.
새 근거 없이 같은 지점을 세 번째 공격하지 않는다.

> 공정 자체도 공격 대상이다. 절차를 지켰다는 이유로 결과를 통과시키지 않는다.

---

# 3. 단계별 절차

## STEP 0 — CONTEXT LOCK

현재 필요한 정보만 잠근다.
- 이번 결정의 목적
- 현재 채널/프로젝트 단계
- 시간·비용·장비·사람 제약
- 이미 확정된 결정
- 가장 중요한 미확인 전제

과거 기록 전체를 읽지 않는다.
`AGENTS.md`가 지정한 필수 부팅 + 현재 작업에 필요한 파일만 본다.

### Exit Attack
> 과거 자료가 현재 문제의 프레임을 오염시키고 있지 않은가?

---

## STEP 1 — PROBLEM FRAME

아이디어를 말하기 전에 문제를 한 줄로 쓴다.

> `누구에게 / 어떤 궁금증·문제를 / 어떤 결과로 해결해야 하는가?`

함께 잠글 것:
- 성공 조건
- 실패 조건
- 현재 해결책을 하지 않아도 되는 `do nothing` 기준

### Exit Attack
> 문제 정의 안에 이미 특정 해결책을 정답처럼 끼워 넣지 않았는가?

---

## STEP 2 — DIVERGE

표준 주요 기획은 가능하면 **서로 다른 원리의 후보 3개**를 만든다.

기본 구성:
- **A 최소안** — 가장 단순하고 제작 통제가 쉬움
- **B 공격안** — 클릭·재미·차별성의 상방을 크게 노림
- **C 다른 원리안** — A/B와 다른 메커니즘으로 목적을 해결

같은 아이디어의 제목·장식만 바꿔 3안이라고 하지 않는다.
사용자나 AI가 처음 던진 안을 기준점으로 대우하지 않는다.

각 후보는 짧게 적는다.
- 한 줄 아이디어
- 클릭 질문
- 실제 발생 사건
- 결과가 나오는 방식
- 대략적인 제작비용/의존성

### Exit Attack
> 후보가 실제로 다른가? 상방이 큰 안을 미리 안전하게 만들지 않았는가?

---

## STEP 3 — COMPETE 1

처음부터 긴 점수표를 돌리지 않는다.
먼저 **Kill Gate**만 본다.

1. 클릭 질문이 존재하는가
2. 결과가 애매해도 콘텐츠가 남는가
3. 사건이 구조상 발생하는가, 우연만 기다리는가
4. 제작시간/섭외에 상한을 둘 수 있는가
5. 사용자가 실제로 지속 수행 가능한가

명백히 실패하면 제거한다.
불확실하지만 상방이 큰 안은 KILL하지 말고 TEST 후보로 남긴다.
상위 1~2개만 심층 공격한다.

### Exit Attack
> 가장 무난한 안만 살아남고 가장 강한 안을 불확실성 때문에 죽인 것은 아닌가?

---

## STEP 4 — RED TEAM A: 시청자/콘텐츠

원안을 살릴 의무가 없다.

> `이 영상이 망했다면 시청자는 왜 안 눌렀고, 왜 나갔는가?`

공격 축:
- 클릭 이유
- 초반 30~60초 이탈 이유
- 중간 반복 피로
- 사건 밀도
- 결과 내구성
- 개인 기록에 갇히는 문제
- 이 채널/이 사용자여야 하는 이유
- 차별성과 상방이 실제로 있는가

분류:
- **치명적** — 수리 못 하면 제거
- **수리 가능** — 구조 변경
- **허용 가능** — 해결비용이 더 큼
- **불확실** — 말로 결정하지 말고 TEST

### Exit Attack
> 문제 수를 많이 찾은 것을 좋은 검토라고 착각하지 않았는가?

---

## STEP 5 — REPAIR

치명적·수리 가능한 문제만 고친다.

수리 순서:
1. 구조
2. 사건 선택/순서
3. 촬영 규칙 단순화
4. 편집 정보 공개 방식
5. 마지막에 장식·장비·추가 절차

함께 적는다.
- 고친 것
- 포기한 것
- 새로 생긴 비용

원안을 보존할 의무가 없다.
수리 중 더 강한 새 후보가 나오면 갈아탄다.

### Exit Attack
> 문제를 해결한 것이 아니라 설명·장식으로 가렸거나 다른 곳으로 옮긴 것 아닌가?

---

## STEP 6 — RED TEAM B: 제작/사업/증거/기회비용

RED TEAM A를 반복하지 않는다.

공격 축:
- 제작시간 상한
- 반복 생산성
- 성장/수익화/채널 자산성
- 사람·장소·우연 의존성
- 실패했을 때의 비용
- 근거가 실측인지 추론인지
- 같은 효과를 더 싸게 얻는 방법
- 지금 이걸 하는 기회비용

마지막 질문:
> **`이 아이디어가 GO인 것`과 `지금 다음 제작 슬롯을 이 아이디어에 쓰는 것이 최선인 것`은 같은가?**

### Exit Attack
> 효율을 본다는 이유로 재미·차별성·상방을 깎아 평균안으로 만들지 않았는가?

---

## STEP 7 — TEST DECISION

핵심 불확실성을 말로 더 토론하는 것보다 실측이 싸다면 여기서 테스트한다.
심층 공정을 끝까지 채우려고 기다리지 않는다.

테스트 우선순위:
> **의사결정을 바꿀 정보량 ÷ 시간·비용**

`가장 싼 테스트`가 아니라 **가장 싸게 중요한 결정을 바꾸는 테스트**를 고른다.

테스트 전 잠글 것:
- 가설
- 관찰 항목
- PASS
- FAIL
- 애매할 때 다음 행동
- 시간/비용 상한
- 결과가 어떤 결정을 바꾸는지

결과를 본 뒤 기준을 유리하게 바꾸지 않는다.
기준 변경이 필요하면 원래 기준을 남기고 이유를 기록한다.

### Exit Attack
> 측정하기 쉬운 것을 재고 있고, 정작 판정을 바꾸는 불확실성은 피해가고 있지 않은가?

---

## STEP 8 — COMPETE 2

수정안을 자동 승격하지 않는다.

다시 비교:
- 남은 경쟁안
- 수리 과정에서 새로 나온 안
- 더 단순한 안
- 아무것도 하지 않는 안

질문:
> `지금 가진 정보로 다음 제작 슬롯의 기대값이 가장 높은 선택은 무엇인가?`

### Exit Attack
> 이미 시간을 썼다는 이유로 수정안에 매몰되고 있지 않은가?

---

## STEP 9 — AI VERDICT

AI 판정은 네 개만 사용한다.

- **GO** — 현재 정보에서 실행 기대값이 가장 높음
- **TEST** — 중요한 불확실성을 실제로 확인해야 함
- **HOLD** — 가능하지만 지금 우선순위가 아님
- **KILL** — 현재 전제/근거에서는 구조적으로 약함

`KILL`은 영구 금지가 아니다.
기존 KILL 근거를 깨는 새 증거가 생겨야 재개한다.

### Exit Attack
> 판정이 근거가 아니라 자신감의 표현이 되지 않았는가?

---

## STEP 10 — USER DECISION

AI 판정과 사용자 결정을 분리한다.

```text
AI 판정: GO
사용자 결정: 보류
```

가능하다.

`DECISIONS.md`의 `확정`은 사용자가 확정했거나 실행을 승인했을 때만 쓴다.

---

## STEP 11 — RECORD

단일 소유권:

| 내용 | 파일 |
|---|---|
| 실패 방지 행동 규칙 | `PROJECT_RULES.md` |
| 기획 검수 절차 | `PLANNING_PROCESS.md` |
| 장기 판단 원칙 | `JUDGMENT_CORE.md` |
| 판단 사례 | `JUDGMENT_CASES.md` |
| 사용자 장기 맥락 | `USER_CONTEXT.md` |
| 현재 상태/다음 행동 | `진행상태.md` |
| 사용자 결정 | `DECISIONS.md` |
| 예측/TEST/실제 결과/교정 | `VALIDATION_LOG.md` |
| 특정 콘텐츠 상세 | 해당 계획 파일 |

같은 현재 사실을 여러 파일에 복사하지 않는다.
다른 문서에는 필요한 경우 짧은 참조만 둔다.

---

## STEP 12 — CALIBRATE

주요 GO/TEST는 가능하면 실행 전에 `VALIDATION_LOG.md`에 남긴다.
- 핵심 가정
- 예측
- 가장 불확실한 전제
- 반증 조건
- 실제로 볼 지표/관찰

실제 결과 후:
1. 예상과 실제 비교
2. 맞은 판단 / 틀린 판단 분리
3. 오차 원인을 `정보 부족 / 원칙 오류 / 실행 실패 / 외부 변수 / 복합`으로 구분
4. 재사용 가능한 교훈만 `JUDGMENT_CORE.md`에 승격
5. 특정 사례는 `JUDGMENT_CASES.md`에 남김

> 판단 체계의 목적은 과거 결론을 지키는 것이 아니라 **예측 오차를 줄이는 것**이다.

---

# 4. 같은 모델 RED TEAM의 한계

같은 모델이 생성과 공격을 모두 하면 완전한 독립성은 없다.
따라서 `독립 RED TEAM`이라고 부르지 않고 **분리 RED TEAM**이라고 한다.

고영향 결정에서 fresh-context 검토가 가능하면 검토자에게 가능하면 다음만 준다.
- 문제 정의
- 제약
- 후보안
- 핵심 근거

원 제안자의 긴 변호는 가능한 한 제외한다.

fresh-context 검토를 사용할 수 없다고 작업을 멈추지 않는다.
동일 모델이라는 한계를 인정하고, 큰 비용을 쓰기 전에 실제 TEST 비용을 낮춘다.

---

# 5. 증거 등급

- **[실측]** 직접 수치·파일·실험으로 확인
- **[사용자 확정]** 사용자가 명시적으로 결정하거나 경험을 보고
- **[관찰]** 실제 화면/자료에서 확인했지만 원인까지 확정하지 않음
- **[추론]** 현재 근거에서 가장 가능성 높은 해석
- **[가설]** 아직 시험 전

추론/가설을 실측처럼 말하지 않는다.
접근하지 못한 파일·영상·도구 결과를 확인했다고 말하지 않는다.

---

# 6. 사용자 제출 형식

주요 기획은 원초적 초안이 아니라 검수된 형태로 낸다.

```text
[문제]
[비교한 경쟁안]
[최종 제안]
[죽을 뻔한 이유]
[수리한 부분]
[판정을 바꿀 남은 불확실성]
[GO / TEST / HOLD / KILL]
[다음 행동]
```

단순 사안은 압축한다.
형식 채우기가 목적이 아니다.

---

# 7. 공정 자체의 정기 공격

다음 신호가 나오면 공정을 줄이거나 고친다.
- 기획 문서 작성 시간이 실제 테스트보다 길어짐
- 모든 안이 비슷한 안전안으로 수렴
- 같은 공격 항목이 매번 형식적으로 반복
- 테스트 결과가 나와도 판단 원칙이 업데이트되지 않음
- 새 세션 부팅 문서가 다시 비대해짐
- 사용자가 또 AI 초안의 QA 역할을 반복하게 됨

수정 방식도 동일하다.

```text
문제 발견
→ 수정
→ Stage Exit Attack
→ 새 결함 있으면 한 번 재수정
→ 실제 작업에서 사용
→ 결과 후 교정
```

---

# 8. 최종 목적

> **약한 아이디어는 큰 비용을 쓰기 전에 죽이고, 강한 아이디어의 상방은 보존하고, 실제 결과를 통해 다음 판단을 더 정확하게 만든다.**

사용자의 역할은 AI 초안을 계속 고치는 것이 아니라 **최종 판단과 실제 제작**이어야 한다.
