# AGENTS.md — 필수 시작 절차

목적: 새 세션이 **현재 상태 + 사용자 맥락 + 핵심 판단 원칙**을 빠르게 복원하되, 과거 사례를 전부 읽어 컨텍스트를 무겁게 만들지 않는다.

## 1. 기본 부팅 — 항상 읽기

이 저장소에서 작업하기 전에 아래 파일을 순서대로 읽는다.

1. `CLAUDE.md` — Karpathy 공통 상위 행동 규칙
2. `PROJECT_RULES.md` — 까막눈 프로젝트 전용 실패 방지 규칙
3. `JUDGMENT_CORE.md` — 오래 유지할 핵심 판단 원칙
4. `USER_CONTEXT.md` — 사용자 의도·취향·협업 방식
5. `진행상태.md` — 현재 최우선 과제와 바로 다음 행동
6. `DECISIONS.md` — 사용자 확정·보류·철회 결정

**필수 부팅에서 `JUDGMENT_CASES.md`, `VALIDATION_LOG.md`, 과거 기록 전체를 읽지 않는다.**
필요할 때만 조회한다.

---

## 2. 작업 종류별 추가 조회

### 새 포맷/후속편/큰 구조 변경 등 주요 기획
`PLANNING_PROCESS.md`를 읽고 그 공정을 따른다.

### 과거와 비슷한 판단 문제 / 왜 이런 원칙이 생겼는지 필요
`JUDGMENT_CASES.md`에서 관련 사례만 조회한다.

### TEST 설계 / GO 전 예측 / 실제 결과 사후평가
`VALIDATION_LOG.md`를 조회·갱신한다.

### 현재 첫 콘텐츠
`첫콘텐츠_계획.md`를 추가로 읽는다.

### 장비/녹화
`장비세팅.md`를 추가로 읽는다.

### 과거 반복듣기/실측/도구
필요한 경우에만 `계획.md`, `실측/`, `도구/`, `기록/`을 조회한다.

---

## 3. 우선순위

- 시스템/개발자/현재 사용자 지시가 저장소 문서보다 우선한다.
- `CLAUDE.md`의 Karpathy 규칙은 저장소 공통 상위 행동 규칙이다.
- `PROJECT_RULES.md`는 프로젝트 실패 방지 규칙이다.
- `JUDGMENT_CORE.md`는 판단 원칙이지 현재 사실 목록이 아니다.
- 현재 사실은 `진행상태.md`, 사용자 결정은 `DECISIONS.md`를 우선한다.
- 과거 사례는 현재와 유사할 때만 참고한다.
- 새 실측이 과거 판단과 충돌하면 과거 판단을 방어하지 않는다.

---

## 4. 주요 기획 제출 규칙

사용자가 별도로 `공격해봐`라고 말하기를 기다리지 않는다.
주요 기획은 `PLANNING_PROCESS.md`에 따라 **경쟁안 생성 → 분리 RED TEAM → 수리 → 다른 축 RED TEAM → 필요 시 사전 기준 TEST → 최종 경쟁**을 거친 뒤 제시한다.

각 주요 단계가 끝날 때 `Stage Exit Attack`을 수행해:
- 목적을 실제로 전진시켰는지
- 새 결함을 만들지 않았는지
- 숨은 가정이 없는지
- 더 단순한 길이 없는지

확인한다.

---

## 5. 문서 단일 소유권

같은 현재 사실을 여러 문서에 복사하지 않는다.

- 행동/실패 방지 규칙 → `PROJECT_RULES.md`
- 기획 검수 절차 → `PLANNING_PROCESS.md`
- 장기 판단 원칙 → `JUDGMENT_CORE.md`
- 판단 사례 → `JUDGMENT_CASES.md`
- 사용자 장기 맥락 → `USER_CONTEXT.md`
- 현재 상태/다음 행동 → `진행상태.md`
- 사용자 결정 → `DECISIONS.md`
- 예측/테스트/실제 결과/교정 → `VALIDATION_LOG.md`
- 특정 영상 상세 → 해당 계획 파일

`JUDGMENT.md`는 과거 링크 호환용 인덱스이며 새 내용을 넣지 않는다.

---

## 6. CLAUDE.md 보호

`CLAUDE.md`의 `BEGIN IMMUTABLE KARPATHY GUIDELINES` ~ `END IMMUTABLE KARPATHY GUIDELINES` 블록은 수정·삭제·요약·재작성·재배열하지 않는다.
새 프로젝트 규칙은 `PROJECT_RULES.md`에만 추가한다.

---

## 7. 작업 종료 전

의미 있는 변경이 있을 때만 해당 소유 문서를 갱신한다.
모든 문서를 습관적으로 갱신하지 않는다.

특히:
- 사용자 확정이 없는데 AI 판정을 `DECISIONS.md`의 `확정`으로 쓰지 않는다.
- 현재 상태를 `JUDGMENT_CORE.md`에 넣지 않는다.
- 사례를 필수 부팅 문서로 승격시키지 않는다.
- 문서 정리가 실제 촬영·편집보다 더 큰 일이 되면 중단하고 실행으로 돌아간다.
