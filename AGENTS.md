# AGENTS.md — 까막눈 프로젝트 입구

이 저장소는 `까막눈` 프로젝트의 현재 작업을 이어가기 위한 최소 운영 지식만 둔다.

## 1. 시작 순서

프로젝트 연속성에 의존하는 요청(`이어가자`, `지금 어디까지`, `다음 뭐야`)은 답변 전에 다음 순서로 확인한다.

1. `STATE.md`
2. 현재 요청에 실제로 필요한 문서만 추가

요청별 추가 문서:
- 첫 콘텐츠 세부 → `FIRST_VIDEO.md`
- 큰 기획·판단 → `PLAYBOOK.md`
- 프로젝트의 안정적 배경·채널 정체성 → `PROJECT_CONTEXT.md`
- 사용자 취향·성향·지속 가능성 등 **사용자 적합성이 판단을 바꿀 때** → `USER_PROFILE.md`

모든 문서를 일괄 preload하지 않는다.

## 2. 현재 상태의 권위

- 현재 프로젝트 상태와 다음 행동의 기준은 `STATE.md`다.
- 첫 콘텐츠의 현재 세부 기준은 `FIRST_VIDEO.md`다.
- 안정적인 사용자 특성의 기준은 `USER_PROFILE.md`다.
- ChatGPT personalization memory, 이전 채팅 기억, 모델의 추측은 current truth가 아니다.
- repo와 기억이 충돌하면 repo의 현재 owner를 우선한다.

GitHub/repo 접근이 불가능하면 현재 상태를 복원했다고 말하지 않는다. 과거 기억으로 빈칸을 채우지 말고 `repo 확인 불가`라고 명시한다.

## 3. 과거 자료 차단

현재 `main`은 clean-sheet 운영면이다.

- backup branch, git history, 과거 문서, 폐기 문서는 기본 retrieval 대상이 아니다.
- 사용자가 명시적으로 `예전 기록 찾아줘`, `왜 그렇게 정했지`, `복구해줘`라고 요청한 경우에만 과거 자료를 조회한다.
- 과거 자료에서 찾은 내용은 현재 owner가 확인하지 않는 한 current fact로 승격하지 않는다.

## 4. 외부 사실

모든 사용자 요청은 먼저 웹에서 현재 정보를 확인한다. 가능하면 공식 문서·1차 자료·연구를 우선한다. 사용자가 해당 요청에서 웹 검색을 금지했거나 웹 도구가 실제로 unavailable인 경우만 예외다.

## 5. 수정 원칙

- 현재 상태가 바뀌면 `STATE.md`만 갱신한다.
- 첫 콘텐츠 세부가 바뀌면 `FIRST_VIDEO.md`만 갱신한다.
- 오래 유지될 프로젝트 정체성·목표·구조는 `PROJECT_CONTEXT.md`에 넣는다.
- 오래 유지되고 의사결정을 실제로 바꾸는 비민감 사용자 취향·성향은 `USER_PROFILE.md`에 넣는다.
- 오래 유지될 판단/작업 원칙만 `PLAYBOOK.md`에 넣는다.
- 새 문서는 기존 owner로 명확히 해결할 수 없을 때만 만든다.
- 일회성 대화, 테스트 문구, 가상 수치, 실패 사례를 자동으로 영구 지식으로 만들지 않는다.
- `USER_PROFILE.md`를 취향 수집함으로 만들지 않는다. 현재 판단에 영향 없는 사소한 선호는 저장하지 않는다.

## 6. 책임 분리

사용자는 목표·제약·취향·최종 선택을 정한다.
AI는 기술 구조, 검색·메모리 방식, 근거 판단, 검증 방법 같은 엔진 결정을 최신 근거를 바탕으로 스스로 판단한다. 사용자에게 내부 아키텍처 선택을 떠넘기지 않는다.

## 7. 후속 협업 연속성

- 사용자가 요약·재확인·근거 설명을 요청하지 않은 후속 작업에서는 이미 확정된 사실·판단을 배경으로만 사용하고 반복 설명하지 않는다.
- 답변은 현재 작업 지점에서 새 판단·새 근거·새 구체안·다음 행동을 우선한다.
- 사용자가 일부를 교정하면 해당 변경점에서 계속하며, 필요하지 않으면 전체 설명을 처음부터 재구성하지 않는다.
- 기획 요청에서는 방법론·프레임을 다시 설명하기보다 적용된 구체 결과를 우선 제시한다.
