# STATE.md — 현재 상태

기준 시각: 2026-08-24 KST

## 현재 상태

`main`은 기존 문서 체계를 버리고 clean-sheet 구조로 재구축된 상태다.
현재 `main`의 운영 문서는 최소 구조만 유지한다.

하지만 **일반 새 채팅 cold-start는 FAIL**했다.

새 채팅은 현재 `main`의 `STATE.md`를 기준으로 복원하지 않고, 과거 채팅/메모리에서 나온 것으로 보이는 오래된 카타카나 1편·2편 상태와 옛 작업방식 개편안을 현재 상태처럼 서술했다.

현재 `main`에는 그 답변이 주장한 `까막눈/04_상태/...`, `까막눈/03_계획/작업방식_개편계획.md` 같은 active 경로가 존재하지 않는다.
현재 `main` HEAD는 clean-sheet rebuild 커밋이다.

## 이번 실패에서 확정된 것

문제는 `main`에 옛 문서가 남아서가 아니다.

**repo 안의 `AGENTS.md`는 새 채팅이 먼저 그 repo를 선택하고 파일을 읽어야만 효력이 생긴다.**
일반 새 채팅이 과거 ChatGPT memory / chat history를 먼저 사용하면, repo-local 규칙만으로는 그 첫 진입을 보장할 수 없다.

따라서 이 문제를 해결하기 위해 repo 문서를 더 추가하거나 과거 문서를 다시 수리하지 않는다.

## 다음 한 단계 — 외부 bootstrap 경계 만들기

새로운 **ChatGPT Project**를 하나 만들고, 가능하면 **Project-only memory**를 사용한다.
기존 까막눈 관련 과거 채팅은 이 새 Project로 옮기지 않는다.

Project instruction은 짧게 유지한다.

```text
까막눈 프로젝트의 현재 상태를 이어가는 요청에서는 항상 GitHub repository `jungjuhyun/kkamaknun`의 `main`을 확인한다.
먼저 `STATE.md`를 읽고, 거기서 필요한 현재 문서만 추가로 읽는다.
Git history, backup branch, 과거 채팅/메모리는 사용자가 명시적으로 과거 기록을 요청한 경우에만 사용한다.
repo를 확인하지 못하면 현재 상태를 추측하지 말고 확인 불가라고 말한다.
```

그 뒤 새 Project 안에서 새 채팅 하나를 열고 다음 문장만 보낸다.

> 까막눈 프로젝트 이어서 하자. 지금 어디까지 왔고 다음에 뭘 해야 하는지 확인해줘.

이 테스트에서 current `STATE.md`와 `FIRST_VIDEO.md`를 기준으로 복원되면 시스템 작업을 종료한다.

## 현재 first content

현재 first-content 기준은 `FIRST_VIDEO.md`다.

- 카타카나 영상이 아니다.
- 애니를 자막 없이 보고 실제로 들린 소리와 이해한 뜻을 기록한다.
- 후편집에서 실제 대사와 대조해 일본어 학습 시작점을 진단한다.
- 유력 작품은 귀멸의 칼날 TV 애니.
- 정확한 화수는 아직 최종 확정 전.

## 통과 후 즉시 제작 복귀

1. OBS 오디오 3트랙 20초 녹화 → 실제 편집 프로그램에서 분리 확인
2. 카메라 눈높이·각도 확정
3. 사용할 작품/화수 최종 확정
4. `말하기 전에 SPACE`를 포함한 약 10분 실촬영 테스트
5. 60~90초 시험 편집
6. 실제 자막 대조·진단 분류 확인
7. 본촬영

## 시스템 작업 종료 원칙

이번 실패를 이유로 repo architecture를 다시 확장하지 않는다.
필요한 수정은 repo 내부가 아니라 **새 세션이 어디서 출발할지 정하는 외부 bootstrap 경계**다.
이 한 번의 Project cold-start 이후 memory/context 시스템 작업을 종료하고 제작으로 복귀한다.
