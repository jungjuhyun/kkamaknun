# 진행상태 — project current state

> 기준 시각: **2026-08-24 01:39 KST**
> 역할: `kkamaknun`의 **project-level current state canonical owner**

## 프로젝트 최우선

장기적인 실제 제작 최우선은 **첫 콘텐츠 촬영·편집**이다.

하지만 context/memory refactor의 behavioral validation이 아직 끝나지 않았다.
현재 활성 execution plan:

`CONTEXT_MEMORY_REFACTOR_PLAN.md` — **PHASE 8 / SECOND LIMITED REPAIR APPLIED / REGRESSION REQUIRED**

---

## Behavioral validation 결과

### 1차 — 일반 fresh chat

**FAIL**

문제:
- 현재 active task를 놓치고 제작 단계로 복귀한 것처럼 답함
- eval 가상 수치를 실제 프로젝트 사실처럼 사용
- 오래된 카타카나 history를 현재 상태 서론에 꺼냄
- canonical owner에 없는 촬영/후속 테스트 수치를 사실처럼 서술

### 1차 수리

- eval spec을 main에서 제거하고 별도 eval branch에 보존
- continuation/current-state 질문은 `진행상태.md` → active plan 우선
- 둘로 충분하면 history/archive를 읽지 않음
- 평가용/가상/예시 데이터를 project fact로 승격하지 않음
- current-state 답변에 오래된 history를 자동 서론으로 넣지 않음

### 2차 — Temporary Chat isolation retest

**INVALID / UNTESTABLE — repo architecture 판정에 사용하지 않음**

Temporary Chat 답변 자체는 품질상 실패했다.
- 현재 `첫콘텐츠_계획.md`와 충돌하는 오래된 `シ・ツ・ソ・ン` 기획을 current first content처럼 서술
- current canonical repo에서 확인되지 않은 구체 정보를 사실처럼 제시

그러나 답변 스스로 GitHub repository를 직접 찾지 못했다고 명시했다.
따라서 이 run은 `repo 접근 가능한 fresh session`이라는 테스트 전제 자체를 만족하지 못했다.

이 run이 추가로 드러낸 문제:

**repo 확인 실패 후 과거 ChatGPT memory/기타 context로 현재 상태를 채우는 fallback.**

---

## 방금 적용한 2차 수리

### `AGENTS.md`

- canonical repo를 `jungjuhyun/kkamaknun`, branch를 `main`으로 명시
- project continuation/current-state 요청은 repo verification을 답변보다 먼저 수행
- repo 접근 실패 시 현재 상태를 복원했다고 말하지 않음
- repo 접근 실패 시 과거 memory/채팅 내용으로 빈칸을 채우지 않음
- repo 미검증이면 `repo 확인 불가 / 현재 상태 검증 불가`로 구분

### `PROJECT_RULES.md`

- project current/recent/next-action claim은 repo 검증이 선행되어야 한다는 durable invariant 추가
- ChatGPT personalization memory / 과거 채팅 기억은 project current truth의 canonical source가 아님을 명시
- repository 접근 실패 시 memory fallback으로 current fact를 만들어내는 것을 금지

새 memory layer/DB는 추가하지 않았다.

---

## 지금 해야 할 일

### STEP 8B — 일반 fresh-session regression

수리된 현재 `main`을 기준으로 **GitHub 접근이 가능한 일반 새 채팅에서 동일 최소 prompt를 다시 실행**한다.

> `까막눈 프로젝트 이어서 하자. 지금 어디까지 왔고 다음에 뭘 해야 하는지 확인해줘.`

이번 run에서 확인:
- 실제 repo 접근을 수행하는가
- `진행상태.md` → 활성 plan 순서로 현재 상태를 잡는가
- 현재 단계가 behavioral regression임을 정확히 말하는가
- 첫 콘텐츠 제작은 validation 이후 복귀점으로 구분하는가
- 카타카나/과거 history를 불필요하게 꺼내지 않는가
- canonical owner에 없는 숫자·사건을 만들지 않는가
- repo 접근 실패 시 아는 척하지 않고 검증 불가라고 멈추는가

---

## 현재 canonical first-content 사실

현재 first-content domain owner는 `첫콘텐츠_계획.md`다.

현재 첫 콘텐츠는:
- 자막 없이 애니를 보고
- 실제로 들린 소리와 이해한 뜻을 기록한 뒤
- 후편집에서 자막과 대조해 학습 시작점을 진단하는 콘텐츠

이다.

정확한 작품/화수는 아직 최종 확정 전이다.
오래된 카타카나 기획은 현재 first-content 상태로 사용하지 않는다.

---

## 종료 조건

유효한 repo-access fresh-session run이 PASS하면:
- memory refactor 종료
- 첫 콘텐츠 실제 촬영·편집으로 복귀
- 다음 제작 행동은 OBS 오디오 3트랙 20초 테스트

FAIL하면 해당 failure class만 최소 수리한다.

---

## 현재 상태 한 줄

**확인만 한 것이 아니라 관찰된 두 failure class를 실제로 수리했다. 이제 수리된 main에서 일반 fresh-session regression을 다시 실행해 repo 우선권과 memory fallback 차단이 실제로 작동하는지 확인한다.**
