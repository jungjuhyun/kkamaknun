# 진행상태 — project current state

> 기준 시각: **2026-08-24 01:24 KST**
> 역할: `kkamaknun`의 **project-level current state canonical owner**

## 프로젝트 최우선

장기적인 실제 제작 최우선은 **첫 콘텐츠 촬영·편집**이다.

하지만 context/memory refactor의 첫 natural fresh-session behavioral test가 **FAIL**했다.
따라서 production으로 넘어가기 전에 **제한적 수리 후 isolation retest**를 먼저 끝낸다.

현재 활성 execution plan:

`CONTEXT_MEMORY_REFACTOR_PLAN.md` — **PHASE 8 / LIMITED REPAIR + RETEST**

---

## 방금 확인된 실패

새 채팅에 다음 최소 prompt만 전달했다.

> `까막눈 프로젝트 이어서 하자. 지금 어디까지 왔고 다음에 뭘 해야 하는지 확인해줘.`

fresh answer의 문제:

- 현재 active task가 behavioral validation인데 이미 첫 콘텐츠 편집 단계로 복귀한 것처럼 답함
- blind eval의 가상 `35분 / 8개 / 14분 vs 8분` 시나리오를 실제 프로젝트 사실처럼 사용
- 현재 질문에 필요 없는 오래된 카타카나 맥락을 꺼냄
- current canonical owner에서 확인되지 않은 추가 촬영/후속 테스트 수치를 실제 진행 사실처럼 서술

판정:

**BEHAVIORAL FAIL — routing/current-state + eval contamination + unsupported claim**

---

## 적용한 최소 수리

1. blind eval spec은 `eval/context-memory-spec-20260824` branch에 보존하고 `main`에서는 제거했다.
2. `AGENTS.md`에서 continuation/current-state 질문은:
   - `진행상태.md`
   - 활성 execution plan
   을 먼저 읽고 충분하면 history/archive를 읽지 않도록 강화했다.
3. 평가용·가상·예시·테스트 시나리오는 canonical owner 확인 없이 project fact로 사용하지 못하게 했다.
4. current-state 답변에서 오래된 history를 자동 서론으로 꺼내지 않게 했다.

새 DB/layer는 추가하지 않았다.

---

## 지금 해야 할 일

### STEP 8A — Temporary Chat isolation retest

일반 새 채팅은 ChatGPT personalization memory / past-chat context의 영향을 받을 수 있다.
따라서 다음은 **Temporary Chat**으로 분리한다.

Temporary Chat에서 GitHub 접근이 실제로 가능하면 같은 최소 prompt를 사용한다.

> `까막눈 프로젝트 이어서 하자. 지금 어디까지 왔고 다음에 뭘 해야 하는지 확인해줘.`

확인할 것:
- 현재 단계가 memory behavioral retest임을 정확히 찾는가
- 첫 콘텐츠 제작은 retest 통과 후 복귀점으로 구분하는가
- 35분/8개/14분 vs 8분 같은 eval 가상 데이터를 실제 사실로 사용하지 않는가
- 카타카나 같은 오래된 history를 현재 상태 설명에 불필요하게 꺼내지 않는가
- canonical owner에 없는 숫자/사건을 만들지 않는가

Temporary Chat에서 GitHub 접근 자체가 안 되면 그것을 architecture FAIL로 판정하지 않는다.

### STEP 8B — 일반 새 채팅 regression

Temporary Chat이 PASS한 뒤 일반 새 채팅에서도 같은 prompt로 한 번 확인한다.

---

## 종료 조건

둘 다 PASS하면:
- memory refactor 종료
- 첫 콘텐츠 실제 촬영·편집으로 복귀
- 다음 제작 행동은 OBS 오디오 3트랙 20초 테스트

실패하면 해당 failure class만 다시 수리한다.

---

## 현재 상태 한 줄

**첫 natural fresh-session test는 실패했다. eval 명세가 main repo를 오염시키고 current-state routing도 충분히 강하지 않았던 문제를 최소 수정했다. 지금 다음 단계는 Temporary Chat에서 GitHub 기반 isolation retest다.**
