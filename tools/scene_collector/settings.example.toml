# 실제 작업 데이터 위치는 사용자가 로컬 settings.toml에서 지정한다.
# 사용자 SSD의 드라이브 문자나 절대경로를 이 예제에 넣지 않는다.
# 빈 값은 허용하지 않으므로 복사한 뒤 실제 값을 입력한다.
[storage]
work_data_dir = ""

# 실제 서비스와 모델은 설정에서 선택한다.
# 작업 3에서 실제 AI 연결을 검증하기 전까지 이 값은 연결 확인용 설정이다.
[ai]
service = ""
model = ""
