# 필수: 사용자가 관리하는 기존 작업 데이터 디렉터리의 절대경로를 입력한다.
# 저장소 예제에는 실제 SSD 경로나 드라이브 문자를 넣지 않는다.
[storage]
work_data_dir = ""

# 필수: 이후 AI 연결 작업에서 사용할 서비스와 모델의 식별자를 입력한다.
[ai]
service = ""
model = ""

# AI 후보 수는 3~5, 후보별 Nadeshiko 조회량은 1~20 범위로 입력한다.
[search]
candidate_count = 5
nadeshiko_take = 5
