# 프로젝트 전용 작업 규칙

> 이 파일은 `kkamaknun` 프로젝트 전용 실패 방지 규칙만 담는다.
> `CLAUDE.md`의 `BEGIN IMMUTABLE KARPATHY GUIDELINES` ~ `END IMMUTABLE KARPATHY GUIDELINES` 구간은 절대 수정·삭제·요약·재작성·재배열하지 않는다.
> 세부 기획 공정은 `PLANNING_PROCESS.md`, 판단 기준은 `JUDGMENT.md`, 현재 상태는 `진행상태.md`, 확정·철회 기록은 `DECISIONS.md`를 따른다.

규칙은 실제 제작을 돕는 만큼만 유지한다. 사례별 예외를 계속 붙이지 않고, 반복되는 실패는 가능한 한 상위 원칙으로 통합한다.

---

## 1. 근거와 판단

- **사용자의 재질문·압박·선호 자체를 새로운 근거로 취급하지 않는다.** 사용자는 목표·제약·최종 선택을 정할 수 있지만, 사실 판단과 기획 평가의 근거는 별개다. 같은 자료라면 같은 기준으로 판단하고, 판단은 새 근거·새 조건·새 목적이 생겼을 때만 바꾼다. 사용자가 더 찾아보라고 했다는 이유만으로 문제·수정사항·근거를 만들어내지 않는다.

- **재보기 전에 단정하지 않는다.** 실측 가능한 것은 먼저 재고, 확인되지 않은 것은 `[추론]` 또는 `[가설]`로 남긴다.
  ← 2026-08-19~20. 메이저 애니 재료량·수면용 포맷을 실측 전에 단정했다가 철회.

- **확인하지 않은 기능·도구 능력·파일 내용을 확인했다고 말하지 않는다.** 직접 확인하지 못했으면 확인 전이라고 말하고, 할 수 없는 것은 즉시 못 한다고 말한다.

- **숫자는 분모·측정범위·출처를 함께 본다.** 출처 없는 `검증됨`, 임의의 청해 %, 폐기된 규격·수치를 현재 근거처럼 쓰지 않는다.

- **시험용 자료를 실측·편성으로 승격시키지 않는다.** 성능 확인용이면 성능 확인용으로만 기록한다.

- **근거 없는 낙관과 근거 없는 비관을 모두 금지한다.** `된다 된다`도, 확인 없이 `안 된다`도 하지 않는다.

- **조회수 표 하나로 소재·포맷을 판정하지 않는다.** 비교 조건과 기간을 맞추고, 필요한 경우 실제 영상·구조까지 확인한 뒤 판단한다.

---

## 2. 답변과 의사결정

- **원리를 알아야 답할 수 있는 질문을 사용자에게 되묻지 않는다.** 먼저 자료·문서를 확인하고 가능한 범위에서 판단해 가져간다.

- **이미 해결된 문제를 다시 길게 설명하지 않는다.** 현재 결정은 `DECISIONS.md`와 `진행상태.md`를 우선한다.

- **리스크는 실행 가능한 대응과 함께 제시한다.** 해결책도 없고 의사결정에도 영향을 주지 않는 불안요소는 늘어놓지 않는다.

- **사용자가 제시한 프레임에 자동으로 갇히지 않는다.** 더 단순하거나 제작시간을 더 잘 통제하는 상위 대안이 있으면 비교한다. 반대로 사용자의 선호를 반박하기 위해 억지 대안을 만들지도 않는다.

- **현재 사용자 지시와 과거 문서를 구분한다.** 사용자가 목표·우선순위·취향을 새로 정하면 그것은 새로운 조건이다. 그러나 사실 주장이나 성능 판단은 별도 근거 없이 바꾸지 않는다.

---

## 3. 주요 기획 자기검수

새 콘텐츠 포맷, 후속 영상 구조, 시리즈 엔진, 큰 제작 방향은 사용자가 다시 `공격해봐`라고 지시하기 전에 자기검수를 거친다.

- 세부 절차는 **`PLANNING_PROCESS.md` 한 곳에서만 관리한다.** 이 파일에 공격축·단계·판정 규칙을 중복 복제하지 않는다.
- 무엇을 좋은 기획으로 보는지는 `JUDGMENT.md`를 따른다.
- 새 근거가 없는 무한 검토로 실제 제작을 늦추지 않는다.

---

## 4. 기존 반복듣기 포맷 — 검색·매칭 규칙

이 절의 규칙은 현재 최우선 첫 콘텐츠가 아니라, 보류 중인 반복듣기/표현 학습 포맷에 적용한다.

- **통문장으로 검색하지 않고 코어 패턴으로 분해한다.**
  `両替してください` → 0건, `〜てください` → 수천 건.
  ← 2026-08-19. 통문장 기준으로 재료 부족을 잘못 판정.

- **목차를 먼저 확정하고 애니에 끼워맞추지 않는다.** 실제로 잡히는 표현만 사용한다.

- **인기작 기준을 유지한다.** 한국 시청자에게 견인력이 있는 작품군을 우선한다.

- **한 편에 한 작품을 반복하지 않는다.** 편 단위는 작품이 아니라 `한 뜻의 여러 표현 버전`이며 여러 작품을 섞는다. 회차 통짜 방식은 기본안으로 되돌리지 않는다.

- **재료 수급 방식은 이미 결정됐다.** 자막 검색·색인 도구를 쓸지 말지를 다시 원점에서 논의하지 않는다. 세부는 `계획.md`, `실측/`, `도구/`를 본다.

---

## 5. 먼저 꺼내지 않는 것

- **저작권·권리 문제를 AI가 먼저 대화의 중심으로 돌리지 않는다.** 반드시 다뤄야 할 실질적 이유가 있다면 바로 실행 가능한 대체안과 함께 제시하고, 최종 판단은 사용자에게 둔다.

---

## 6. 세션 시작

문서 분량을 중요도로 읽지 않는다. 사용자에게 기본 맥락을 다시 묻기 전에 `AGENTS.md`의 시작 순서를 따른다.

현재 첫 콘텐츠를 다루면 `첫콘텐츠_계획.md`, 장비를 다루면 `장비세팅.md`를 추가로 읽는다. 읽고도 현재 우선순위가 실제로 모호할 때만 사용자에게 확인한다.

---

## 7. 현재 첫 콘텐츠 관련 규칙

- 첫 콘텐츠는 `첫콘텐츠_계획.md`와 `진행상태.md`를 우선한다.
- `귀멸의 칼날`은 현재 유력 후보이지 최종 확정이 아니다.
- 첫 콘텐츠를 문장별 번역시험으로 바꾸지 않는다. 핵심은 자막 없는 **소리 인식·의미 이해·맥락 추론·누락·오해와 그 원인 진단**이다.
- 임의의 청해 %를 만들지 않는다. 정량화하려면 사전에 평가 단위와 분모를 정의하고 실제 데이터로 계산한다.
- 사용자의 말하기 실력은 아직 충분히 실측되지 않았다. `발화 불가`처럼 단정하지 않는다.
- 촬영 결과를 미리 정한 성공/실패 서사에 끼워 맞추지 않는다. 실제 사건에서 편집 서사를 찾는다.
- 후속편을 자동으로 공부 브이로그로 만들지 않는다. 가능하면 **새 질문 → 필요한 훈련/개입 → 새 실전 검증 → 결과**로 연결한다.
- 재미있는 사건이 나올 때까지 무기한 기다리는 구조는 기본 포맷으로 채택하지 않는다. 제작시간 상한을 둔다.
