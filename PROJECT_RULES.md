# 프로젝트 전용 작업 규칙

> 이 파일은 `kkamaknun` 프로젝트 전용 **실패 방지 규칙**만 담는다.
> `CLAUDE.md`의 `BEGIN IMMUTABLE KARPATHY GUIDELINES` ~ `END IMMUTABLE KARPATHY GUIDELINES` 구간은 절대 수정·삭제·요약·재작성·재배열하지 않는다.
> 세션 시작 순서는 `AGENTS.md`, 기획 공정은 `PLANNING_PROCESS.md`, 판단 원칙은 `JUDGMENT_CORE.md`가 소유한다.

까막눈 일본어 학습·실험 유튜브 프로젝트.
규칙이 늘어나면 그 자체가 병목이 된다. 앞 프로젝트는 지침 7,000줄을 쓰고 영상 0편으로 끝났다.

---

## 1. 꺼내지 않는 것

- **저작권·권리 참견 금지.** AI가 먼저 꺼내지 않는다. 소재·화면·클립 선택을 권리 문제로 되돌리지 않는다.
  꺼낼 수 있는 유일한 경우는 **바로 쓸 수 있는 대체안을 같이 낼 때**다. 판단은 사용자 소관이다.
  ← 2026-08-19. 하루에 세 번 어겼다.

- **재료 수급 방식은 결정됐다.** 기존 반복듣기 포맷에서 자막 검색/색인 도구를 쓸지 말지를 다시 논의하지 않는다.
  ← 2026-08-19.

- **원리를 알아야 답할 수 있는 질문은 사용자에게 묻지 않는다.** 사용자는 엔진 원리를 모른다고 명시했다.
  선택지를 던지고 판단을 떠넘기지 말고 먼저 조사·판단해서 가져간다.

- **확인하지 않은 기능·도구 능력을 가능하다고 말하지 않는다.** 직접 확인하지 못했으면 확인 전이라고 말한다.
  못 하는 것은 즉시 못 한다고 말한다.

- **이미 해결된 문제를 다시 길게 설명하지 않는다.** 현재 결정이 있으면 `DECISIONS.md`와 `진행상태.md`를 우선한다.

---

## 2. 검색·매칭 규칙 — 기존 반복듣기 포맷

- **통문장으로 검색하지 않는다. 코어 패턴으로 분해해서 검색한다.**
  `両替してください` → 0건. `〜てください` → 수천 건.
  ← 2026-08-19. 통문장으로 재고 "재료가 없다"는 틀린 결론을 냈다. 실제로는 통문장 73% 실패 → 코어패턴 2% 실패.

- **목차를 먼저 확정하고 애니에 끼워맞추지 않는다.** 걸린 것만 쓰고 나머지는 버린다.
  교재 목차를 채울 의무가 없다. 15개 중 3개가 안 걸리면 그 3개를 버리면 된다.
  ← 2026-08-19. 목차부터 깔고 재서 "안 된다"는 결론을 두 번 냈다.

- **인기작 기준.** 한국인이 아는 작품만 쓴다. 지브리·신카이·귀멸 등.
  안 유명한 애니는 접객 대사가 많아도 쓰지 않는다. 견인력이 IP에서 나온다.
  ← 2026-08-19. 진쌤이 아따맘마 한 작품으로 26편 연속 내고 EP1 429,540 → EP37 6,369으로 죽었다.

- **한 편에 한 작품을 반복하지 않는다.** 반복듣기 편 단위는 작품이 아니라 **표현(한 뜻의 여러 말투 버전)**이고,
  작품군으로 묶어 여러 작품을 섞는다. 회차 통짜는 남들이 하다 죽은 방식이다.
  ← 2026-08-19 '상황'→'표현' 정정.

---

## 3. 말하기 전

- **재보기 전에 단정하지 않는다.** "메이저 애니엔 생활 장면이 없다"고 단정했다가 실제 측정과 충돌했다.
  소비 상황·실측을 먼저 본다.

- 숫자를 말할 때는 **어디서 나온 숫자인지** 같이 말한다. 출처 없이 `검증됨`이라고 쓰지 않는다.

- **"된다 된다" 금지.** 근거 없이 낙관하지 않는다. 반대로 실측 없이 비관하지도 않는다.

- **조회수 표만 보고 소재·포맷을 판정하지 않는다.** 비교 대상의 성격·기간·실제 영상 구조를 확인한 뒤 말한다.

- **폐기된 규격·수치를 인용하지 않는다.** 최신 계획에 아직 살아 있는지 먼저 확인한다.

- **시험용 자료를 실측·편성으로 승격시키지 않는다.** 성능 확인용이었으면 그렇게 적는다.

- **리스크를 꺼낼 때는 바로 쓸 수 있는 해결책까지 같이 낸다.** 해결책 없이 불안요소만 추가하지 않는다.

- **사용자가 던진 아이디어 하나에 매몰되지 않는다.** 더 강하거나 더 단순한 다른 원리의 대안을 함께 경쟁시킨다.

---

## 4. 주요 기획은 사용자에게 내기 전에 자기검수한다

사용자가 매번 `공격해봐`라고 말하게 만들지 않는다.
새 콘텐츠 포맷, 후속 영상 구조, 시리즈 엔진, 큰 제작 방향은 `PLANNING_PROCESS.md`를 따른다.

실패 방지 핵심만 이 파일에 둔다.
- 같은 모델의 공격을 `독립 RED TEAM`이라고 과장하지 않는다.
- 하나의 원안을 계속 수리하는 데 매몰되지 않고 서로 다른 원리의 후보를 경쟁시킨다.
- 각 주요 단계가 끝날 때 **Stage Exit Attack**을 수행한다.
- 공격이 기획의 상방·차별성을 다 깎아 안전한 평균안으로 만들지 않게 한다.
- TEST는 결과를 본 뒤 기준을 바꾸지 않도록 가설/PASS/FAIL/비용 상한을 먼저 잠근다.
- AI의 `GO/TEST/HOLD/KILL`은 사용자 결정과 분리한다.
- 새 근거 없이 공격을 무한 반복해 실제 제작을 늦추지 않는다.

---

## 5. 세션 시작/문서 조회

- 새 세션은 `AGENTS.md`의 기본 부팅 순서만 따른다.
- 과거 판단 사례·검증 로그·과거 기록 전체를 습관적으로 읽지 않는다.
- 현재 문제와 관련 있을 때만 `JUDGMENT_CASES.md`, `VALIDATION_LOG.md`, `기록/`, `실측/`, `계획.md`를 조회한다.
- 문서 분량을 중요도로 읽지 않는다. 파일이 많다고 현재 주력이 아니다.
- 읽고도 현재 우선순위가 실제로 모호할 때만 사용자에게 확인한다.

← 2026-08-19. 과거 문서 분량 때문에 현재 주력을 잘못 추정.
← 2026-08-20. 세션 상태 파일 없이 긴 대화 원문으로 복구.

---

## 6. 문서 중복 금지

같은 현재 사실·결정을 여러 파일에 복사해 세션을 무겁게 만들지 않는다.

소유 문서는 `AGENTS.md`의 `문서 단일 소유권` 표를 따른다.
다른 문서에서 필요하면 **링크/한 줄 참조**만 둔다.

특히:
- 현재 상태를 `JUDGMENT_CORE.md`에 넣지 않는다.
- 과거 사례를 `USER_CONTEXT.md`에 복사하지 않는다.
- AI 추천을 사용자 `확정`으로 기록하지 않는다.
- `JUDGMENT.md`는 호환 인덱스일 뿐 새 내용을 쓰지 않는다.

---

## 7. 현재 첫 콘텐츠 관련 실패 방지 규칙

- 첫 콘텐츠는 `첫콘텐츠_계획.md`와 `진행상태.md`를 우선한다.
- HelloTalk에서 재미있는 사건이 나올 때까지 기다리는 방식은 첫 포맷에서 후순위다. 제작시간 통제가 안 된다.
- `귀멸의 칼날`은 현재 유력 후보이지 최종 확정이 아니다. 확정처럼 말하지 않는다.
- 첫 콘텐츠를 문장별 번역시험으로 바꾸지 않는다. 핵심은 자막 없는 **소리 인식·의미 이해·맥락 추론·누락·오해와 그 원인 진단**이다.
- 임의의 청해 %를 만들지 않는다. 정량화할 거면 사전 항목과 실제 데이터가 필요하다.
- 사용자의 말하기 실력은 아직 충분히 실측되지 않았다. `발화 불가`라고 단정하지 않는다.
- 촬영 결과를 미리 `초반 성공 → 중반 실패 → 후반 회복` 같은 서사에 끼워 맞추지 않는다. 실제 사건에서 편집 서사를 찾는다.
- 후속편을 자동으로 공부 브이로그로 만들지 않는다. 가능하면 **새 질문 → 필요한 개입 → 새 실전 검증 → 결과**로 본다.
